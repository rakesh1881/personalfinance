package com.flightbooking.form;

import java.awt.Dimension;
import java.io.File;
import java.net.URL;
import java.security.CodeSource;

import javax.swing.ImageIcon;
import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JPanel;

import com.flightbooking.main.ApplicationStart;

public class MainMenu extends JFrame{

	JPanel mainMenuPanel;
	JButton btnMaster,btnTransaction,btnReports,btnStocks,btnNetworth;
	
	public MainMenu() {
		
		setTitle("Main Menu");
		setPreferredSize(new Dimension(1000,600));
		setBounds(0, 0, 1000, 600);
		setLayout(null);
		setLocationRelativeTo(null);
		setDefaultCloseOperation(EXIT_ON_CLOSE);
		createMenus();
		setVisible(true);
		
	}

	private void createMenus() {
		
		mainMenuPanel= new JPanel();
		mainMenuPanel.setBounds(0,0,1000,600);
		mainMenuPanel.setLayout(null);
		add(mainMenuPanel);
		
		URL url = ApplicationStart.class.getProtectionDomain().getCodeSource().getLocation();
		File file =new File(url.getFile());
		String path = file.getAbsolutePath().substring(0,file.getAbsolutePath().length()-4);
		System.out.println(path);
		ImageIcon icon = new ImageIcon(path+"\\icons\\masterIcon.png");
		btnMaster = new JButton("");
		btnMaster.setBounds(10, 10, 180, 180);
		btnMaster.setIcon(icon);
		mainMenuPanel.add(btnMaster);
	}

}
